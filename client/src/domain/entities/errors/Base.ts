/**
 * Domain Error — own class describing exceptions in our business-logic.
 * For example, when some resource is not found, or user is not authorized.
 */
export default class DomainError extends Error {}
